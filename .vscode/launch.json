{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Generate Static",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/builder",
            "args": [
                "-generate_static"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}
